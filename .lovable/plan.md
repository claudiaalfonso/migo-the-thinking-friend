

## Update WhatsApp number across all components

Replace the `"REPLACE_ME"` placeholder with `"12084084168"` in three files:

1. **`src/components/FloatingWhatsApp.tsx`** — line with `const WHATSAPP_NUMBER = "REPLACE_ME"`
2. **`src/components/landing/Hero.tsx`** — line with `const WHATSAPP_NUMBER = "REPLACE_ME"`
3. **`src/components/landing/Pilot.tsx`** — line with `const WHATSAPP_NUMBER = "REPLACE_ME"`

All `wa.me` links will then correctly open WhatsApp with your Migo AI agent number. No other changes needed.

